namespace WowPacketParser.Enums
{
    public enum GossipOptionIcon
    {
        Gossip                      = 0,  // white chat bubble
        Vendor                      = 1,  // brown bag
        Taxi                        = 2,  // flightmarker (paperplane)
        Trainer                     = 3,  // brown book (trainer)
        SpiritHealer                = 4,  // golden interaction wheel
        Inkeeper                    = 5,  // golden interaction wheel
        Banker                      = 6,  // brown bag (with gold coin in lower corner)
        Petition                    = 7,  // white chat bubble (with "..." inside)
        Tabard                      = 8,  // white tabard
        Battlemaster                = 9,  // two crossed swords
        Auctioneer                  = 10, // yellow dot/point
        TalentMaster                = 11, // white chat bubble
        StableMaster                = 12, // white chat bubble
        PetSpecialization           = 13, // white chat bubble
        GuildBanker                 = 14, //
        Spellclick                  = 15, //
        DisableXpGain               = 16, // white chat bubble
        EnableXpGain                = 17, // white chat bubble
        Mailbox                     = 18, // white chat bubble
        WorldPvpQueue               = 19, // white chat bubble
        DungeonFinder               = 20, // white chat bubble
        ArtifactRespec              = 21, // white chat bubble
        CemeterySelect              = 22,
        SpecializationMaster        = 23, // white chat bubble
        GlyphMaster                 = 24, // white chat bubble
        ScenarioQueue               = 25, // white chat bubble
        GarrisonArchitect           = 26,
        GarrisonMissionNpc          = 27,
        GarrisonShipmentNpc         = 28,
        GarrisonTradeskillWorkOrder = 29, // white chat bubble
        GarrisonRecruitementNpc     = 30, // white chat bubble
        AdventureMap                = 31, // white chat bubble used for command table (bfa)
        GarrisonTalentNpc           = 32, // white chat bubble
        ContributionCollector       = 33, // white chat bubble
        TransmogrifyNpc             = 34,
        AzeriteItemRespec           = 35,
        IslandQueueNpc              = 36,
        UiInteractionNpc            = 37,
        WorldMapNpc                 = 38,
        UnkGossipIcon39             = 39,
        ChromieTime                 = 40,
        RuneforgeCrafting           = 42,
        GuideRecruiter              = 43,
        RuneforgeUpgrading          = 44,
        CovenantRenown              = 45,
    }
}
